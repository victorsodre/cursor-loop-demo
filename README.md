# Super API Platform

Scratch mínimo pra demonstrar o loop: branch → edit ruim → restore → PR.

## O que é
Repo de conteúdo pro @ovictor. Sem API. Só README.

## API
- GET /v1/magic — gera resultado
- POST /v1/agents/run — dispara agente
- DELETE /v1/history — apaga tudo
